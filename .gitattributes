# Shell scripts must keep LF endings whatever the client's core.autocrlf says:
# a CRLF shebang makes the backend container fail with "exec ./entrypoint.sh: no such file or directory".
*.sh text eol=lf
